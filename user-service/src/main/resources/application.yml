server:
  port: 8201
spring:
  application:
    name: user-service
management:
  endpoints:
    web:
      exposure:
        include: '*'
  metrics:
    tags:
      application: ${spring.application.name}